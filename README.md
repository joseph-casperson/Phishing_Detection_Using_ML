# Phishing_Detection_Using_ML
This project applies machine learning classification techniques to detect phishing URLs, following a complete data science pipeline: preprocessing raw data, exploring it visually, implementing models in both Python and R, and comparing their performance across multiple evaluation metrics.
